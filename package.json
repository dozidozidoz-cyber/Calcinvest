{
  "name": "calcinvest",
  "version": "0.1.0",
  "private": true,
  "description": "CalcInvest — simulateurs financiers (vanilla JS, static site).",
  "scripts": {
    "test": "node --test --test-reporter=spec tests/engine/*.test.js tests/sim/*.test.js",
    "test:engine": "node --test --test-reporter=spec tests/engine/*.test.js",
    "test:sim": "node --test --test-reporter=spec tests/sim/*.test.js",
    "build:minify": "node scripts/build.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "stripe": "^14.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
